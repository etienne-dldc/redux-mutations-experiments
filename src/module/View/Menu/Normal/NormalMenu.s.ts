import styled from 'styled-components';
import { Sizes } from 'module/View/Common';

export const Wrapper = styled.menu`
  position: fixed;
  top: 0;
  left: 0;
  right: 0;
  width: ${Sizes.leftMenuWidth}px;
`;
