import * as React from 'react';
import { createPureComponent } from 'module/Common';

type Props = {};

export const PostView = createPureComponent<Props>('PostView', ({}) => {
  return <div>Post</div>;
});
